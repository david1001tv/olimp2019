import autobind from 'autobind-decorator';
import {smartSetHeight} from '../../../utils';

const CELL_WIDTH = 60;
const CELL_BORDER = 60;

export default class FillWordsComponent {
    constructor(posX, posY, game) {

    }
}